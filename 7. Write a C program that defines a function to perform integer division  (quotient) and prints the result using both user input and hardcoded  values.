#include <stdio.h> 
int divide(int x, int y) { 
    int result = x / y; 
    return result; 
} 
int main() { 
    int a, b; 
    scanf("%d%d", &a, &b); 
    int quotient = divide(a, b); 
    printf("Quotient=%d\n", quotient); 
    printf("Quotient=%d\n", divide(100, 4)); 
    return 0; 
} 
